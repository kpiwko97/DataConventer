@model Users[]
    <!DOCTYPE html>
    <html>
    <head>
        <title>title</title>
    </head>
    <body> 
    <table class="table table-striped">
        <thead>
        <tr>
            <th>EmployeeType </th>
            <th>Id</th>
            <th>Email</th>
            <th>Gender</th>
            <th>EndDate</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var a in @Model)
        {
            <tr>
                <td>@a.EmployeeType</td>
                <td>@a.Id</td>
                <td>@a.Email</td>
                <td>@a.Gender</td>
                <td>@a.EndDate</td>
            </tr>
        }
        </tbody>
    </table>

    </body>

</html>
